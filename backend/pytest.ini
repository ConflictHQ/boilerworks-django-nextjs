[pytest]
DJANGO_SETTINGS_MODULE = config.settings
addopts = --strict-markers -p no:warnings

python_files = tests.py test_*.py *_tests.py

markers =
  functional: Run functional tests for graphql operations

log_cli = True
